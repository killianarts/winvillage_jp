# Control Flow and Routing with HTMX and Django

One of the things I'm learning is how to deal with control flow and
routing in Django when using HTMX.

I'm working on a form that needs to update some data in one part when
another part is modified. This is a typical requirement of forms and
HTMX uses two methods to achieve that effect: events and out-of-band
swaps. In both cases,

## The Vanilla Django Way
